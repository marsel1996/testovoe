export type UserData = {
    id ?: number | null,
    firstName?: string | null,
    surName?: string | null,
    email?: string | null,
    phone?: string | null,
    roleId?: number | null,
    password?: string | undefined,
}